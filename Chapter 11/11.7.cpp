#include<map>
#include <string>
#include <vector>

using namespace std;

int main(int argc, char const *argv[])
{
	map<string, std::vector<string> > family;
	std::vector<string> name;
	string family_name;
	family.insert({family_name, name});

	return 0;
}
